# EQR6 工具箱 · 界面设计

**版本**：v2.0 设计稿
**设计原则**：单手可用、信息密度高、不用翻菜单就能看到关键状态

---

## 0. 整体结构

```
┌─────────────────────────────────┐
│  底部四个标签页，拇指可达         │
├─────────────────────────────────┤
│  🏠 首页      服务器总览 + 快捷工具 │
│  📄 日志      浏览 EQR6 日志文件    │
│  ⚙️ 服务      计划任务开关          │
│  🔧 设置      服务器地址/密钥/Tailscale │
└─────────────────────────────────┘
```

**为什么是底部标签**：手机单手操作，底部最容易点。四个是上限（再多就挤）。

---

## 1. Tailscale 提醒（**你特别要求的**）

这是**贯穿全 app 的机制**，不是单独一个页面。

### 1.1 三处提醒

**① 首页顶部横幅**（只在没开时出现）

```
┌─────────────────────────────────┐
│ ⚠️ Tailscale 未开启              │
│ 在外网访问需要它。点此去开启 →    │
└─────────────────────────────────┘
```

- **黄色背景**，醒目但不吓人
- 点一下**尝试直接启动 Tailscale**（能启动就启动，不能就跳设置页）
- **Tailscale 开着时这条完全不显示**（不占地方）

**② 加载失败时的提示**

请求 EQR6 失败时，不是干巴巴报个错，而是**先判断是不是 Tailscale 的问题**：

```
┌─────────────────────────────────┐
│ ❌ 连不上 EQR6                    │
│                                 │
│ 可能原因：                        │
│  • Tailscale 没开  ← 点这里检查   │
│  • 服务器地址填错  ← 点这里改     │
│  • EQR6 关机了                    │
│                                 │
│ 当前地址：100.77.117.76:8080      │
│ [ 重试 ]  [ 换地址 ]              │
└─────────────────────────────────┘
```

**③ 设置页的 Tailscale 卡片**

```
┌─────────────────────────────────┐
│ Tailscale                        │
│ 状态：✅ 已连接                   │
│ 本机 IP：100.86.152.81           │
│ EQR6  ：100.77.117.76            │
│ [ 打开 Tailscale ]               │
└─────────────────────────────────┘
```

**如果没开**：
```
│ 状态：❌ 未连接                   │
│ [ 去开启 Tailscale ]  ← 主按钮    │
```

### 1.2 怎么检测 Tailscale 是否在运行

**方法**：检查本机有没有 `100.x.y.z` 的地址。

```kotlin
// 遍历网络接口，找 100.64.0.0/10 网段的 IPv4 地址
NetworkInterface.getNetworkInterfaces()
  → 过滤出 100.64.0.0/10 范围内的地址
  → 有 = Tailscale 已连接
```

**优点**：不依赖 Tailscale app 的接口，纯本地判断，**零权限**。

### 1.3 怎么"打开 Tailscale"

Tailscale 的 Android 包名是 `com.tailscale.ipn`。用 Intent 尝试拉起：

```kotlin
val intent = packageManager.getLaunchIntentForPackage("com.tailscale.ipn")
if (intent != null) startActivity(intent)
else 跳转到应用商店 / 提示手动打开
```

> **说明**：无法用代码"打开 VPN 开关"（那是系统权限），**只能拉起 app 让你手动点**。
> 所以提醒的措辞是"点此去开启"，而不是"一键开启"。

---

## 2. 🏠 首页

```
┌─────────────────────────────────┐
│ EQR6 工具箱              [刷新]  │
├─────────────────────────────────┤
│ ⚠️ Tailscale 未开启 →（仅在未开时）│
├─────────────────────────────────┤
│  ┌───────────────────────────┐  │
│  │ 🖥️ EQR6 状态         ● 在线│  │
│  │                           │  │
│  │  内存   ████████░░  62%   │  │
│  │  磁盘C  ██████░░░░  27%   │  │
│  │  磁盘D  █████████░  41%   │  │
│  │                           │  │
│  │  已开机 14 小时            │  │
│  └───────────────────────────┘  │
├─────────────────────────────────┤
│  快捷工具                        │
│  ┌──────────┐ ┌──────────┐     │
│  │ 🚀       │ │ 📄       │     │
│  │ 打开DSH  │ │ 看日志    │     │
│  └──────────┘ └──────────┘     │
│  ┌──────────┐ ┌──────────┐     │
│  │ ⚙️       │ │ 📦       │     │
│  │ 服务管理 │ │ 版本管理  │     │
│  └──────────┘ └──────────┘     │
├─────────────────────────────────┤
│  服务快照                        │
│  DSH web        ✅ 运行中        │
│  更新服务        ✅ 运行中        │
│  SSH            ✅ 运行中        │
│  Tailscale      ✅ 运行中        │
│  健康巡逻        ✅ 17/17 通过    │
└─────────────────────────────────┘
```

**设计要点**：

- **下拉刷新**（不用找按钮）
- **进度条用颜色区分**：<70% 绿、70-90% 黄、>90% 红
- **服务快照**一眼看出有没有东西挂了
- 点"打开DSH" → **直接开 DSH 界面**（后台自动取令牌）
- 点服务快照的某一行 → 跳到服务页

---

## 3. 🚀 打开 DSH（点首页卡片进入，或直接跳转）

**这个工具没有独立页面 —— 它是一个动作**：

```
点击 → 显示进度对话框
       ┌───────────────────────┐
       │  正在准备 DSH…         │
       │  ✅ 检查 Tailscale     │
       │  ⏳ 获取登录令牌        │
       │  ○ 打开浏览器          │
       └───────────────────────┘
     → 浏览器打开带令牌的 DSH 地址
```

**如果令牌是旧的**（DSH 重启过，存的令牌失效）：

```
┌─────────────────────────────────┐
│ ⚠️ 令牌可能已失效                │
│ EQR6 的 DSH 已重启，旧令牌无效    │
│                                 │
│ [ 仍要打开 ]  [ 取消 ]           │
└─────────────────────────────────┘
```

**判断依据**：`dsh-web-url.txt` 的修改时间 **早于** DSH 进程的启动时间 → 令牌是旧的。

---

## 4. 📄 日志页

```
┌─────────────────────────────────┐
│ 日志                     [刷新]  │
├─────────────────────────────────┤
│ [全部] [DSH] [巡逻] [App] [其他]│  ← 分类筛选
├─────────────────────────────────┤
│ dsh-bootstrap.log                │
│   2.6 KB · 今天 15:23            │
├─────────────────────────────────┤
│ health-watchdog.log              │
│   1.2 KB · 今天 15:20            │
├─────────────────────────────────┤
│ phase5-smb-20260918-085459.log   │
│   3.1 KB · 今天 08:54            │
├─────────────────────────────────┤
│ tailscale-install.log            │
│   215 KB · 昨天                  │
└─────────────────────────────────┘
```

**点某个文件 → 日志详情页**：

```
┌─────────────────────────────────┐
│ ← health-watchdog.log            │
├─────────────────────────────────┤
│ [最新] [全部]                    │
├─────────────────────────────────┤
│ 2026-09-18 15:20:31 OK 17/17    │
│ 2026-09-18 09:20:15 OK 17/17    │
│ 2026-09-18 03:20:02 OK 17/17    │
│ ...                             │
├─────────────────────────────────┤
│ [ 复制全部 ]  [ 分享 ]           │
└─────────────────────────────────┘
```

**设计要点**：

- 默认只载入**最后 200 行**（大日志文件不会卡）
- **自动高亮 `ERROR` / `FAIL` / `WARN`**（红/黄色）
- 长按可复制单行
- 可以按"只看错误"过滤

---

## 5. ⚙️ 服务页

```
┌─────────────────────────────────┐
│ 服务                     [刷新]  │
├─────────────────────────────────┤
│ 计划任务                         │
│ ┌─────────────────────────────┐ │
│ │ DSH-Web-Autostart            │ │
│ │ DSH 网页服务 · 开机自启        │ │
│ │ 状态: ● 已就绪      [运行][停] │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ EQR6-AppUpdateServer         │ │
│ │ App 更新服务 · 开机自启        │ │
│ │ 状态: ● 运行中      [重启][停] │ │
│ └─────────────────────────────┘ │
│ ┌─────────────────────────────┐ │
│ │ EQR6-Health-Watchdog         │ │
│ │ 健康巡逻 · 每 6 小时          │ │
│ │ 状态: ● 已就绪      [运行][停] │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ 系统服务                         │
│ sshd          ● 运行中   [重启]  │
│ Tailscale     ● 运行中   [重启]  │
└─────────────────────────────────┘
```

**操作确认**（防误触）：

```
┌─────────────────────────────────┐
│ 停止 DSH 网页服务？               │
│                                 │
│ 停止后你将无法通过网页访问 DSH。   │
│ 下次开机仍会自动启动。             │
│                                 │
│ [ 确认停止 ]      [ 取消 ]        │
└─────────────────────────────────┘
```

**设计要点**：

- **危险操作二次确认**（停服务、重启）
- 操作后**显示结果**（不是静默执行）
- **不允许**通过 app 停止 sshd / Tailscale —— 那会切断你唯一的访问通道，**万一出错你就彻底失联了**

> ⚠️ **这条我坚持**：app **不提供**停用 SSH 和 Tailscale 的按钮。
> 只提供"查看状态"和"重启"。理由：这两个是远程访问的生命线，
> 停掉就等于把自己锁在门外，而你人在外面无法恢复。

---

## 6. 🔧 设置页

```
┌─────────────────────────────────┐
│ 设置                             │
├─────────────────────────────────┤
│ 服务器连接                       │
│ ┌─────────────────────────────┐ │
│ │ 服务器地址                   │ │
│ │ [100.77.117.76:8080      ]  │ │
│ │   留空 = 自动选择             │ │
│ │                             │ │
│ │ API 密钥                     │ │
│ │ [••••••••••••••••        ]  │ │
│ │                             │ │
│ │ [ 测试连接 ]                 │ │
│ │ ✅ 连接成功 · 延迟 42ms      │ │
│ └─────────────────────────────┘ │
├─────────────────────────────────┤
│ Tailscale                        │
│ 状态：✅ 已连接                   │
│ 本机 IP：100.86.152.81           │
│ [ 打开 Tailscale ]               │
├─────────────────────────────────┤
│ 更新                             │
│ 当前版本：1.5 (build 6)          │
│ [ 检查更新 ]                     │
│ 🔵 更新服务器：github.com         │
├─────────────────────────────────┤
│ 关于                             │
│ 目标服务器：ptcg-1 (100.77.117.76)│
│ [ 查看版本历史 ]                 │
└─────────────────────────────────┘
```

**设计要点**：

- 服务器地址**留空 = 自动**（依次探测局域网 → Tailscale → GitHub）
- **「测试连接」**：立刻告诉你通不通、延迟多少
- API 密钥**掩码显示**，有个"显示"眼睛图标
- 更新那一块**保留现有逻辑**（v1.5 已经能用）

---

## 7. 配色与风格

| 元素 | 设计 |
|---|---|
| **主色** | 深蓝 `#1565C0`（和信息类应用相称） |
| **成功** | 绿 `#2E7D32` |
| **警告** | 橙 `#EF6C00` |
| **错误** | 红 `#C62828` |
| **背景** | 浅灰 `#F5F5F5`，卡片白色带圆角和轻阴影 |
| **字号** | 标题 22sp / 卡片标题 17sp / 正文 15sp / 辅助 12sp |
| **圆角** | 卡片 12dp，按钮 8dp |

**深色模式**：先不做（后面可以加）。理由：先用起来，**深色模式是锦上添花**，而且现在做会拖慢进度。

**技术限制说明**：目前项目是**零第三方依赖**（只用安卓原生 API）。
这个界面用原生也能做，只是代码量大一些（约 1000-1500 行）。
**如果你想用 Material Design 组件库会更好看**，但会引入 `com.google.android.material` 依赖。

### 需要你决定

| 选项 | 效果 | 代价 |
|---|---|---|
| **继续零依赖**（推荐） | 界面朴素但功能完整、APK 小（约 700KB） | 视觉普通 |
| **引入 Material 库** | 好看的卡片/涟漪/底部导航 | APK 增大到约 2-3MB，构建多一个依赖 |

**我建议先用零依赖做出来** —— 功能优先。**如果你看过觉得丑，我再换 Material 重做界面**（功能代码不用改）。

---

## 8. 配套的 EQR6 API（我同时要做）

app 需要 EQR6 提供一个接口：

| 接口 | 用途 | 对应工具 |
|---|---|---|
| `GET /api/status` | 内存/磁盘/开机时间/服务状态 | 首页 |
| `GET /api/logs` | 日志文件列表 | 日志页 |
| `GET /api/logs?file=xxx&lines=200` | 读某个日志 | 日志详情 |
| `GET /api/versions` | 版本历史 | 版本管理 |
| `GET /api/services` | 计划任务与服务状态 | 服务页 |
| `POST /api/services/<name>/<action>` | 启停/重启 | 服务页 |
| `GET /api/dsh` | 当前 DSH 令牌 + 有效性 | 打开 DSH |

**安全**：
- 需要 **API 密钥**（`X-API-Key` 请求头）
- 扩展现在的 8080 服务（同一个 Python 进程）
- 防火墙规则不变：**只允许局域网 + Tailscale**

---

## 9. 我需要你确认的三件事

**① 界面布局可以吗？** 特别是底部四个标签的分法。

**② 零依赖朴素风 vs Material 好看风？**（我建议先零依赖）

**③ 那条"不允许从 app 停用 SSH/Tailscale"的限制，你同意吗？**
（我强烈建议同意 —— 这是防止你把自己锁在门外）

---

**你确认后我就开始写代码。** 顺序：

```
EQR6 API 服务 → app 界面骨架 → Tailscale 提醒 → 5 个工具 → 构建 → 你手机上验证
```
