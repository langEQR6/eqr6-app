<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!--
      PACKAGE VISIBILITY (Android 11+)
      Since API 30 an app cannot see other packages unless it declares them.
      Without this block getLaunchIntentForPackage("com.tailscale.ipn") returns
      null, which made the app wrongly report "Tailscale not installed" and
      refuse to open it.
      Network-interface based detection never needed this; only launching did.
    -->
    <queries>
        <package android:name="com.tailscale.ipn" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.DeviceDefault.Light">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Hands the downloaded APK to the system package installer as a
          content:// URI. Android 7.0+ forbids passing file:// URIs between
          apps, which is what broke the first OTA attempt.
        -->
        <provider
            android:name=".ApkProvider"
            android:authorities="${applicationId}.apkprovider"
            android:exported="false"
            android:grantUriPermissions="true" />

    </application>

</manifest>
